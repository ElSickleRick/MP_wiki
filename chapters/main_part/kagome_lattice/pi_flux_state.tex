\documentclass[../../../main.tex]{subfiles}

\begin{document}

\subsubsection{Real Space}

This lattice is also a triangular Bravais-lattice \textcolor{red}{that is probably not true}, only now with unit cell doubling (in the direction of $\vec{t}_2$), hence the unit cell has 6 sites. The Wigner-Seitz cell is a stretched hexagon. In the cartoon a different unit cell is shown.

\begin{center}
	\includegraphics[width=\textwidth]{lattice-pi-flux-state.jpeg}
\end{center}

The nearest neighbour vectors remain unchanged

\begin{equation}
	\vec{\delta}_1 = 
	\begin{pmatrix}
		1 \\ 
		0
	\end{pmatrix} \quad
	\vec{\delta}_2 = \frac12
	\begin{pmatrix}
		1 \\
		\sqrt{3}
	\end{pmatrix} \quad
	\vec{\delta}_3 = \frac12 
	\begin{pmatrix}
		1 \\
		-\sqrt{3}
	\end{pmatrix}.
\end{equation}

The new primitve translations are

\begin{equation}
	\vec{t}_1 = 
	\begin{pmatrix}
		2 \\
		0
	\end{pmatrix} \quad
	\vec{t}_2 =
	\begin{pmatrix}
		-2 \\
		2\sqrt{3}
	\end{pmatrix}
\end{equation}

The real space (effective) tight-binding hamiltonian is

\begin{align}
	\begin{split}
		H = t \sum_{i\sigma}\bigg( &- a_{i\sigma}^\dagger c_{i\sigma} - c_{i\sigma}^\dagger b_{i\sigma} + b_{i\sigma}^\dagger a_{i\sigma} + a_{i\sigma}^\dagger d_{i\sigma} + d_{i\sigma}^\dagger e_{i\sigma} + e_{i\sigma}^\dagger f_{i\sigma} + f_{i\sigma}^\dagger d_{i\sigma} \\
		&+ b_{i\sigma}^\dagger c_{i+t_1;\sigma} + b_{i\sigma}^\dagger e_{i+t_1+t_2;\sigma} + f_{i\sigma}^\dagger d_{i+t_1;\sigma} + f_{i\sigma}^\dagger a_{i+t_1;\sigma} + e_{i\sigma}^\dagger c_{i-t_2;\sigma} + \text{h.c.} \bigg)
	\end{split}
\end{align}

\subsubsection{Reciprocal Space}

The reciprocal lattice vectrors are 

\begin{equation}
	\vec{b}_{1} = \pi
	\begin{pmatrix}
		1 \\
		1/\sqrt{3}
	\end{pmatrix} \quad 
	\vec{b}_2 = \pi 
	\begin{pmatrix}
		0 \\
		1/\sqrt{3}
	\end{pmatrix}
\end{equation}

After a Fourier transform the $k$-space Hamiltonian reads

\begin{equation}
	H = t \sum_{q,\sigma} \Psi_{q\sigma}^\dagger 
	\begin{pmatrix}
		0 & e^{iq_2} & -e^{-iq_3} & 0 & e^{-iq_2} & e^{iq_3} \\
		e^{-iq_2} & 0 & 2i\sin(q_1) & e^{iq_2} & 0 & 0 \\
		-e^{iq_3} & -2i\sin(q_1) & 0 & e^{-iq_3} & 0 & 0 \\
		0 & e^{-iq_2} & e^{iq_3} & 0 & e^{iq_2} & e^{-iq_3} \\
		e^{iq_2} & 0 & 0 &  e^{-iq_2} & 0 & 2\cos(q_1) \\
		e^{-iq_3} & 0 & 0 & e^{iq_3} & 2\cos(q_1) & 0 
	\end{pmatrix}
	\Psi_{q\sigma},
\end{equation}

where
\begin{equation}
	\Psi_{q\sigma}^\dagger = 
	\begin{pmatrix}
		a_{q\sigma}^\dagger & b_{q\sigma}^\dagger & c_{q\sigma}^\dagger & e_{q\sigma}^\dagger & f_{q\sigma}^\dagger & d_{q\sigma}^\dagger 
	\end{pmatrix}.
\end{equation}

\textcolor{blue}{If I write this down somewhere serious, I should just label the sites differently.} \\

I used the shorthand $q_i \equiv \vec{q}\cdot\vec{\delta}_i$. 

\subsubsection{dispersion and band trouching poiunts}

The dispersion relation is shwon in the following picture

\begin{center}
	\includegraphics[width = \textwidth]{dispersion_relation_pi_flux_state.png} 
\end{center}

The lowest band (flat band) is doubly degenerate. \\

\textcolor{blue}{Directions on how to obtain the analytical expressions:\\ 
1. write down characteristic polynomial (probably the most annoying step)\\
2. Divide by the two constant roots corresponding to the flat bands (Polynomial of degree 4 remains) \\
3. Use symmetrie of remaining 4 bands (write as product of 2 quadratic polynomials; due to the symmetrie plane, one can guess p from the p-q-formula) \\4. Solve remaining 2 quadratice equations \\
5. ? \\
6. Profit} \\
 
At half filling the lowest three bands are filled, thus the Fermi-energy is between the yellow and green band.

\begin{center}
	\includegraphics[width = \textwidth]{touching_points_4th_band.png}
\end{center}

Shown above is the 4th band (green band). Apparently there are two flavours of touching points with one of each in the first Brillouin zone. They are time revearsel partners. From the numerical data these tow touching points lie at

\begin{equation}
	\vec{K} = \frac{\pi}{4}
	\begin{pmatrix}
		1 \\
		1/\sqrt{3}
	\end{pmatrix}, \quad
	\vec{K}^\prime = -\frac{\pi}{4}
	\begin{pmatrix}
		1 \\
		1/\sqrt{3}
	\end{pmatrix}.
\end{equation}

\subsubsection{Linearization in reciproical space and Projection}

For now I only do the calculations for $\vec{K}$. Use  

\begin{align}
	\vec{K}\cdot\vec{\delta}_1 &= \frac{\pi}{4} \\
	\vec{K}\cdot\vec{\delta}_2 &= \frac{\pi}{4} \\
	\vec{K}\cdot\vec{\delta}_3 &= 0
\end{align}

to obtain the linearized Hamiltonian in the vicinity of $\vec{K}$:


\begin{equation}
	\begin{pmatrix}
		0 & e^{i\frac{\pi}{4}}(1+ip_2) & -1+ip_3 & 0 & e^{-i\frac{\pi}{4}}(1-ip_2) & 1+ip_3 \\
		e^{-i\frac{\pi}{4}}(1-ip_2) & 0 & \sqrt{2}i(1+p_1) & e^{i\frac{\pi}{4}}(1+ip_2) & 0 & 0 \\
		-1-ip_3 & -\sqrt{2}i(1+p_1) & 0 & 1-ip_3 & 0 & 0 \\
		0 & e^{-i\frac{\pi}{4}}(1-ip_2) & 1+ip_3 & 0 & e^{i\frac{\pi}{4}}(1+ip_2) & 1-ip_3\\
		e^{i\frac{\pi}{4}}(1+ip_2) & 0 & 0 & e^{-i\frac{\pi}{4}}(1-ip_2) & 0 & \sqrt{2}(1-p_1) \\
		1-ip_3 & 0 & 0 & 1 + ip_3 & \sqrt{2}(1-p_1) & 0
	\end{pmatrix} 
\end{equation}

\textcolor{blue}{To keep the size reasonable maybe its a good idea to split this up into zeroth and first order in p. Might also be a good choice for storytelling purposes.} \\

The shorthand means

\begin{align*}
	p_i &\equiv \vec{\delta}_i \cdot \vec{p}, 
\end{align*}

also

\begin{equation}
	e^{i\frac{\pi}{4}} = \frac{1+i}{\sqrt{2}}.
\end{equation}

The eigenvalues for the zeroth order (/directly at the $\vec{K}$-point) are 

\begin{align}
	&\varepsilon_1(\vec{K}) = -2t  &&\varepsilon_2(\vec{K}) = -2t &&\varepsilon_3(\vec{K}) = (1-\sqrt{3})t \notag\\
	&\varepsilon_4(\vec{K}) = (1-\sqrt{3})t &&\varepsilon_5(\vec{K}) = (1+\sqrt{3})t &&\varepsilon_6(\vec{K}) = (1+\sqrt{3})t
\end{align}

$\varepsilon_3$ and $\varepsilon_4$ are the relevant ones.

\end{document}
